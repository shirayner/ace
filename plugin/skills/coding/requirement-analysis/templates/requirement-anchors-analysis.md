---
requirement: {{需求一句话概括}}
source: {{需求来源: 文件路径 / URL / 对话描述}}
generated_at: {{ISO时间戳}}
wiki_available: true | false
total_anchors: N
---

# {{需求名}} — 代码锚点分析

> 基于 [prd.md](./prd.md) 的需求拆分结果。
> 澄清记录见 [requirement-issues.md](./issues/requirement-issues.md)。

## 需求概要
<!-- 从 prd.md 提炼，1-2 句话概括核心变更 -->

## 锚点总览

| 锚点 | 类型 | 来源 | 覆盖故事 | 变更概要 |
|------|------|------|---------|---------|
| {{entry-name}} | {{api/mq/job/page}} | 已有 | US-1, US-3 | {{一句话变更描述}} |
| {{new-entry-name}} | {{api/mq/job/page}} | **新增** | US-2 | {{一句话职责描述}} |
<!-- 仅列出经用户确认需要变更的锚点，来源列标注"已有"或"新增" -->

## 锚点变更分析

### {{锚点名}} ({{类型}}) — 已有

- **覆盖故事**: {{US-1, US-3}}
- **关联原因**: {{为什么需要改，与哪个故事的验收条件相关}}
- **当前行为**: {{从 wiki + 源码理解到的现有逻辑，1-2 句业务语言}}
- **目标行为**: {{变更后的产品行为，1-2 句业务语言}}

- **逻辑变更**:
  <!-- 每项有变更则写，无变更则省略该项 -->

  1. 入参变更
     {{新增/修改/删除的入参字段或结构，业务含义 + 是否必填 + 校验规则，伪代码}}

  2. 出参变更
     {{新增/修改/删除的出参字段或结构，业务含义 + 数据来源，伪代码}}

  3. 领域模型变更
     {{新增实体/值对象、已有模型加字段/改关系、状态机新增流转、枚举新增值，伪代码}}

  4. 业务流程变更
     {{处理步骤的增删改，条件分支变化，调用顺序调整，伪代码或步骤描述}}

  5. 其他规则变更
     {{校验规则、异常处理、降级策略、幂等逻辑、权限控制，有则写无则省略}}

- **关键依赖**:
  <!-- 无变更的行省略 -->

  | 类别 | 依赖项 | 影响说明 |
  |------|--------|---------|
  | 入参 | {{字段/参数名}} | {{新增/修改/废弃}} |
  | 领域服务 | {{DomainService 类名}} | {{调用目的 + 是否需要修改}} |
  | 外部接口 | {{HTTP/RPC 接口名}} | {{调用目的 + 是否需要修改}} |
  | 数据库 | {{表/集合名}} | {{读写目的 + 是否需要 DDL}} |
  | 消息队列 | {{Topic/Queue 名}} | {{发送/消费 + 消息体是否变化}} |
  | 配置 | {{配置项/环境变量}} | {{新增/修改 + 用途}} |
  | 文件系统 | {{文件/目录路径}} | {{读写目的 + 是否需要新增模板}} |
  | 上游调用方 | {{调用方锚点名}} | {{受影响的调用场景}} |

### {{锚点名}} ({{类型}}) — 新增

- **覆盖故事**: {{US-2}}
- **关联原因**: {{为什么需要新增锚点，现有锚点无法覆盖哪些验收条件}}
- **职责描述**: {{新锚点的核心职责，1-2 句业务语言}}

- **逻辑设计**:
  <!-- 按需填写，无内容则省略该项 -->

  1. 入参设计
     {{入参字段/结构，业务含义 + 是否必填 + 校验规则，伪代码}}

  2. 出参设计
     {{出参字段/结构，业务含义 + 数据来源，伪代码}}

  3. 领域模型设计
     {{需要新建的实体/值对象/枚举，伪代码}}

  4. 业务流程设计
     {{处理步骤、分支条件、调用顺序，伪代码或步骤描述}}

  5. 其他规则设计
     {{校验规则、异常处理、降级策略、幂等逻辑、权限控制}}

- **关键依赖**:

  | 类别 | 依赖项 | 影响说明 |
  |------|--------|---------|
  | 入参 | {{字段/参数名}} | {{新增}} |
  | 领域服务 | {{DomainService 类名}} | {{调用目的 + 是否需要新增}} |
  | 外部接口 | {{HTTP/RPC 接口名}} | {{调用目的 + 是否需要新增}} |
  | 数据库 | {{表/集合名}} | {{读写目的 + 是否需要新建表}} |
  | 消息队列 | {{Topic/Queue 名}} | {{发送/消费 + 是否需要新建Topic}} |
  | 配置 | {{配置项/环境变量}} | {{新增 + 用途}} |
  | 文件系统 | {{文件/目录路径}} | {{读写目的 + 是否需要新建模板}} |
  | 上游调用方 | {{调用方锚点名}} | {{受影响的调用场景}} |
