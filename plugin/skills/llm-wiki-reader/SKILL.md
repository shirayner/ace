---
name: llm-wiki-reader
description: 指导需求理解与方案设计 Agent 渐进式消费 .ace/wiki/ 知识库。
  三层渐进加载（SUMMARY→INDEX→anchors），软指引策略。
  当用户进行需求评审、方案设计、影响分析时触发。
---

# LLM Wiki Reader

为需求评审 Agent 和方案设计 Agent 提供渐进式加载 .ace/wiki/ 知识库的协议。

## 触发条件

用户消息包含以下任一模式时激活：

| 任务类型 | 关键词/模式 |
|----------|------------|
| 需求理解 | "评审这个需求" / "分析这个 PRD" / "这个功能影响哪些" / "现有的 XX 逻辑是什么" |
| 方案设计 | "设计 XX 方案" / "技术方案" / "XX 功能怎么实现" / "接口设计" |
| 影响分析 | "改了这个会影响哪些" / "XX 的上下游" / "影响范围" |

## 锚点流程

1. 检查项目根目录 `.ace/wiki/` 是否存在
   - **存在** → 进入加载协议
   - **不存在** → 告知用户项目尚未生成 Wiki，建议先用 `llm-wiki-generator` 生成。**不阻塞任务**，回退到代码分析兜底。

2. 检查 wiki 是否过期（如果 anchor frontmatter 含 `generated_at` 和 `source_commit`）
   - 对比当前 HEAD，如果源码有更新 → 标注"wiki 可能过期"，仍加载但降低可信度
   - 无法确定 → 正常加载

## 渐进加载协议（软指引）

以下是推荐路径，Agent 根据上下文自行判断实际加载深度，不强制。

### L1: SUMMARY.md（~1100 tokens，先读）

**内容**: 项目元信息（`business_domain`/`keywords`/`project_type`）、快速查找表（问题→锚点）、核心业务流程（概要+锚点角色）、核心领域模型（定义+关联锚点+关联原因）

**何时读**:
- 几乎所有任务的首选锚点——SUMMARY 包含项目级元信息（业务板块、关键词），帮助 Agent 在最初就判断"这是否是相关项目"
- 需求评审：理解业务全景和现有规则
- 影响分析：从领域模型出发找所有关联锚点

**怎么用**:
1. 先读 frontmatter，匹配 `business_domain` / `keywords` 判断项目相关性
2. 用"快速查找"表定位锚点
3. 未命中 → 读业务流程，找到任务涉及的流程
4. 未命中 → 读领域模型，找到任务涉及的概念
5. 仍无法定位 → 去 L2（INDEX 锚点目录）模糊匹配

### L2: INDEX.md（~200 tokens，SUMMARY 不够时读）

**内容**: 项目描述、锚点目录（按类型分组 + 描述）

**何时读**:
- SUMMARY 快速查找未命中，需要扫描完整锚点列表
- 方案设计前需要了解全部锚点全景
- 需要知道锚点总数和类型分布

**怎么用**:
- 扫描锚点目录，匹配任务关键词定位候选锚点
- 确定目标 anchor 后 → 去 L3

**不需要读 L2 的情况**:
- SUMMARY 快速查找已命中目标锚点
- 任务中明确给出了锚点类名
- 已熟悉项目锚点结构

### L3: anchors/（~2000-5000 tokens/个，需要深入时读）

**内容**: 业务场景、业务输入/输出、业务规则、调用链路、外部依赖、主要实现类、相关锚点

**何时读**:
- 需要理解某个锚点的具体业务规则和约束
- 需要了解接口契约（输入/输出）
- 需要了解调用链和外部依赖（方案设计）

**怎么用**:
- 读取 anchor 完整内容
- 检查 frontmatter 的 `related_business` 和 body 的"相关锚点"章节
- 判断是否需要追踪关联 anchor（见交叉引用追踪规则）

## 场景矩阵

| 任务场景 | 推荐路径 | 理由 |
|----------|---------|------|
| 首次接触项目 | SUMMARY → INDEX（锚点目录） | SUMMARY 提供项目元信息+业务全景，INDEX 补充锚点清单 |
| 理解系统有哪些能力 | SUMMARY（业务流+领域模型） | 业务流和模型直接展示系统能力边界 |
| 评审某个功能的新需求 | SUMMARY（查找表）→ anchor | 快速定位，深读规则 |
| 设计新接口/改造旧接口 | SUMMARY（查找表）→ 目标 anchor → INDEX（关联 anchor） | 快速定位主锚点，INDEX 补充关联锚点 |
| 分析变更影响范围 | SUMMARY（领域模型）→ 所有关联 anchor | 从模型出发找所有消费者 |
| 跨锚点功能调研 | SUMMARY（业务流）→ 流程涉及的全量 anchor | 先端到端，再逐个深挖 |
| 已知锚点名查具体规则 | anchor（直查） | 跳过 SUMMARY/INDEX |

## 交叉引用追踪规则

读完锚点 A 的 anchor 后，检查 frontmatter `related_business` 和 body "相关锚点"章节。

**追踪信号**（建议继续读关联 anchor）:
- 任务涉及多个锚点联动
- 需要理解上下游数据流
- 锚点 A 的规则引用了锚点 B 的产出

**停止信号**（不追踪）:
- 任务只涉及单一锚点
- 已读 anchor 足够回答当前问题
- token 预算紧张

## 异常处理

统一原则：**wiki 是最佳路径不是唯一路径，查不到不阻塞，回退代码分析兜底**。

| 异常 | 处理 |
|------|------|
| `.ace/wiki/` 不存在 | 告知用户，建议用 `llm-wiki-generator` 生成。不阻塞，回退代码分析 |
| SUMMARY.md 查不到关键词 | 退回 INDEX.md 扫描锚点目录模糊匹配。仍失败则告知用户 |
| 指定 anchor 文件不存在 | 提示 wiki 可能不完整，退回 INDEX 找替代锚点 |
| Wiki 过期（generated_at < HEAD commit） | 加载但标注可信度降低 |
| INDEX 锚点数与 anchors/ 文件数不匹配 | 提示缺失，读存在的，缺失标记"未生成" |

## 与 llm-wiki-generator 的协作

- `llm-wiki-generator` 负责**生成** wiki（构建时操作）
- `llm-wiki-reader` 负责**消费** wiki（运行时操作）
- wiki 不存在时，reader 指引用户先用 generator 生成
- generator 的 SKILL.md 末尾引用 reader："消费 Wiki 请用 llm-wiki-reader"
