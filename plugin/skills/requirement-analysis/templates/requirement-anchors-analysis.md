---
requirement: {{需求一句话概括}}
source: {{需求来源: 文件路径 / URL / 对话描述}}
generated_at: {{ISO时间戳}}
wiki_available: true | false
total_anchors: N
---

# {{需求名}} — 代码锚点分析

> 基于 [prd.md](./prd.md) 的需求拆分结果。
> 澄清记录见 [requirement-issues.md](./issues/requirement-issues.md)。

## 需求概要
<!-- 从 prd.md 提炼，1-2 句话概括核心变更 -->

## 锚点总览

| 入口 | 类型 | 覆盖故事 | 变更概要 |
|------|------|---------|---------|
| {{entry-name}} | {{api/mq/job/page}} | US-1, US-3 | {{一句话变更描述}} |
<!-- 仅列出经用户确认需要变更的入口 -->

## 入口变更分析

### {{入口名}} ({{类型}})

- **覆盖故事**: {{US-1, US-3}}
- **关联原因**: {{为什么需要改，与哪个故事的验收条件相关}}
- **当前行为**: {{从 wiki + 源码理解到的现有逻辑，1-2 句业务语言}}
- **目标行为**: {{变更后的产品行为，1-2 句业务语言}}

- **逻辑变更**:
  <!-- 每项有变更则写，无变更则省略该项 -->

  1. 入参变更
     {{新增/修改/删除的入参字段或结构，业务含义 + 是否必填 + 校验规则，伪代码}}

  2. 出参变更
     {{新增/修改/删除的出参字段或结构，业务含义 + 数据来源，伪代码}}

  3. 领域模型变更
     {{新增实体/值对象、已有模型加字段/改关系、状态机新增流转、枚举新增值，伪代码}}

  4. 业务流程变更
     {{处理步骤的增删改，条件分支变化，调用顺序调整，伪代码或步骤描述}}

  5. 其他规则变更
     {{校验规则、异常处理、降级策略、幂等逻辑、权限控制，有则写无则省略}}

- **关键依赖**:
  <!-- 无变更的行省略 -->

  | 类别 | 依赖项 | 影响说明 |
  |------|--------|---------|
  | 入参 | {{字段/参数名}} | {{新增/修改/废弃}} |
  | 领域服务 | {{DomainService 类名}} | {{调用目的 + 是否需要修改}} |
  | 外部接口 | {{HTTP/RPC 接口名}} | {{调用目的 + 是否需要修改}} |
  | 数据库 | {{表/集合名}} | {{读写目的 + 是否需要 DDL}} |
  | 消息队列 | {{Topic/Queue 名}} | {{发送/消费 + 消息体是否变化}} |
  | 配置 | {{配置项/环境变量}} | {{新增/修改 + 用途}} |
  | 文件系统 | {{文件/目录路径}} | {{读写目的 + 是否需要新增模板}} |
  | 上游调用方 | {{调用方入口名}} | {{受影响的调用场景}} |
