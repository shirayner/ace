# Templates · Backend

> 后端 / 后台配置类需求骨架。纯 Skeleton，规则见 `projection-rules.md`。只写业务控制要求，不写配置表结构 / 配置中心实现（违反 R-T1）。Backend 需求可把调用方 / 下游系统 / 运营角色作为 Actor。

## 后端配置 Backend Config

```markdown
## 后端配置

**配置项**
| 配置项 | 含义 | 取值范围 / 默认值 | 可配置角色 | 生效范围 / 时机 |
|--------|------|-------------------|------------|-----------------|
| <名称> | <业务含义> | <范围 / 默认值> | <角色> | <对象 / 即时或延迟> |

**操作流程与权限**
1. <角色> 从<权限入口>进入 → <配置 / 审核 / 发布动作>。
- 批量操作：<支持范围 / 空值三态>
- 权限边界：<谁可查看 / 编辑 / 审核 / 生效>
```

## 非功能需求 Non-Functional

```markdown
## 非功能需求
> 以业务可感知方式表述（"下单 2 秒内响应"而非架构方案）。

| 类别 | 指标 | 目标值 | 测量口径 | 适用范围 |
|------|------|--------|----------|----------|
| 性能 | <响应时间 / 吞吐> | <如 P95 ≤ 2s> | <测量条件> | <页面 / 流程 / 人群> |
| 容量 | <峰值规模> | <目标值> | <估算口径> | <活动 / 常态> |
| 可用性 | <可用率 / 恢复时间> | <目标值> | <统计窗口> | <核心链路> |
| 兼容性 | <端 / 版本 / 浏览器> | <支持范围> | <验收方式> | <功能范围> |
| 合规 | <隐私 / 审计 / 地区要求> | <目标要求> | <判定依据> | <数据 / 人群 / 地区> |
```
