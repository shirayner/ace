# 聚焦回归自检证据（当前 requirement-writing 规则）

产物：`.ace/tasks/fix-prd-projection/artifacts/regression-output.md`
来源：飞书《【PRD】会员页增加权益推荐模块》revision 6418，本地 `feishu-part-01.md` ~ `feishu-part-09.md`（已全量读取）。
范围：只投影「文档信息」+「业务流程」两章，其余章节按本次回归指令不生成（非规则豁免）。

## P0 保真（R-S1~S8）

| 检查 | 结果 | 证据 |
|------|------|------|
| R-S8 上游画像原样投影 | PASS | `需求规模 = Normal`（上游结论），全文 `Large` 出现 0 次；业务域/交付类型按上游原样落入 |
| R-S6 链接可点 | PASS | 10 条绝对 URL；`doc-id` / `<cite` / `Feishu wiki:` 残留 0；UX 链接原文未提供 → 按 R-T9 写 `原文未提供（待确认）`，未伪造 |
| R-S5 deprecated 排除 | PASS | 邮轮 0、船票 0、SailingID 0、「ant 订单」判断 0、「未到航段结束时间」条件 0、已删除线的接送机订单 id 方案 0 |
| R-S5 deferred 只点一句 | PASS | 休息室详情页仅在文档信息「关联 PRD」出现 1 次并标「下迭代承接」，正文不展开其航段/UI/次数规则 |
| R-S1 六项必覆盖 | PASS | 获取待出行行程（步骤 1-2）、订单类型+30 天双过滤（步骤 3）、逐行程判断四类权益（步骤 4 + 2.2）、至少 1 个可用权益才展示入口（步骤 5）、按权益类型聚合（步骤 7）、点 Get 按类型跳转（步骤 8） |
| R-S4 不臆造 | PASS | 2.2/2.3/2.4/2.5 每行可回溯 part-01~03；未给原文没有的默认值 |
| R-T11 同名不同口径 | PASS | 章首显式区分「30 天窗口＝入口露出过滤口径」与「未来 60 天有单＝覆盖面测算口径」 |

## P0 门禁（R-T1 / R-T10）

| 检查 | 结果 | 证据 |
|------|------|------|
| R-T1 不写实现 | PASS | 非 mermaid 代码块 0；SQL/DDL 0、`RecommendationQueryService` 0、`.recommend` 0、`pshowcode` 0、`CRNModuleName` 0、`destcountryid` 0、`segmentNo` 0、`coverImageUrl` 0、`qconfig` 0、埋点名 `ibu_loyalty_app_*` 0、Shark key `Trip_Reward_Component*` 0、`ou_*` 人员 id 0 |
| R-T1 业务语义保留（不因门禁误删） | PASS | 「取导流返回的第一条链接」「city id 带入门票列表页」「30 天做成配置项」「不校验已有 eSIM 订单及其三条理由」按业务语言保留；纯契约字段表剥离为文档信息里的契约链接 |
| R-T10 清占位 | PASS | 角括号匹配仅 `<br>`（表格内换行）；`<!--` 0、`PRD-WORKING` 0、TODO/XXX 0 |
| R-T5/T6/T7/T8 | 不适用 | 成功指标、非目标、REQ+AC、功能总览属本次未投影章节，非豁免声明 |

## P0 流程图门禁（R-T14 / R-T15）

R-T14 特征命中判定（命中即须出图，本产物已出 `flowchart TD`）：

| 特征 | 命中 | 原文依据 |
|------|------|----------|
| 分支 | 是 | 自定义行程走/不走休息室；四类权益四个不同承接页 |
| 过滤 | 是 | 订单类型过滤 + 30 天窗口过滤；中转航段目的地去除 |
| 循环 | 是 | 「对所有通过 30 天窗口过滤的行程，逐条判断 4 个权益」 |
| 回流 | 是 | 权益使用后剩余次数变化 → 下次访问重算露出 |
| 跨角色 / 系统交接 | 是 | 用户 → 会员服务端 → 公共行程接口 → 各权益承接页 |
| 多阶段跳转 | 是 | 行程获取 → 过滤 → 权益判断 → 入口露出 → 浮层 → 承接页 |
| 并行 | 否 | 原文未要求并行判断 |

R-T15 语义一致性逐项比对（非通读印象）：

| 项 | 结果 | 证据 |
|----|------|------|
| 节点 | PASS | 22 个动作/状态节点全部对上正文：S/A/B→步骤 1-2，D/E→步骤 3 两条过滤，G/G1/G2/G3/H→步骤 4 + 2.4 自定义行程行，I/J→步骤 5，K→步骤 6，L→步骤 7，M/N/N1-N4→步骤 8 四条，P→步骤 9 + 2.5，X1/X2/X3→2.4 前三行。正文步骤无一缺图 |
| 条件 | PASS | 6 个菱形判定；34 条边中 15 条带业务条件标签，判定节点出边**无一条空标签**（脚本断言 `UNLABELED edges out of decision nodes = []`）；阈值口径与正文一致：`30 天`（图 2 次 / 正文 3 次，同为访问当日 +30 天）、`至少 1 条候选行程且至少 1 个可用权益`、`no_trip / no_valid_trip / no_benefit` 三码图文各 1 次严格对应 |
| 去向 | PASS | X1/X2/X3 → `结束`，与 2.4「流转到＝结束」一致；`H --> G` 回流指回同一取行程步骤；`P -.-> A` 回流指回行程获取，与步骤 9「再次访问按剩余次数与行程状态重算」一致 |
| 引用 | PASS | 图上未标注 BR-00X / REQ-00X（本次未投影功能需求章），无死引用；脚本断言引用但未声明的节点 id = [] |
| 不引入新事实 | PASS | 42 条节点/边标签逐条可回溯正文；`60 分钟`、`中转` 只在正文 2.2/2.4 承载，图上未出现也未被图改写 |

## P1 / P2

- P1 一致性：权益排序全文统一「休息室 > 免费门票 > 免费 eSIM > 接送机升级」（图 1 次 + 步骤 7 一致）；「行程 / 候选行程 / 待出行行程」术语分层固定；只有 `## 1. 文档信息`、`## 2. 业务流程` 两个一级章，序号连续。
- P1 可扫描性：步骤 4 的四类权益条件、eSIM 常驻地五种组合、异常 12 行、使用后流转 4 行分别下沉为 2.2/2.3/2.4/2.5 四张表，避免单块 >8 行硬塞。
- P2 保真简洁：删元话语与重复；保护集（条件、阈值、例外、口径、来源、原因）全保留，如「同时区判断」「起飞时间 +60min」「去除中转航段」「不校验已有 eSIM 订单的三条理由」「附属订单不进判断池的原因」。

## 可复跑断言

`python .ace/tasks/fix-prd-projection/artifacts/verify_prd_projection.py` → `Projection assertions: 6/6`，退出码 0（校验 Skill 规则侧四文件，非本产物）。

## 环境说明（非产物问题）

回归执行期间，另一进程把 `.ace/tasks/fix-prd-projection/` 归档到 `.ace/tasks/archive/2026-08-12-fix-prd-projection/`，一度清空了指定的落盘目录。已按指令要求的原路径重建目录，并把产物与来源分块复制回 `.ace/tasks/fix-prd-projection/artifacts/`。未修改任何 Skill 文件、state.json。
